"""Testable engine components. A module may import contracts, ports, platform,
config and models — never a sibling module and never a concrete provider. Enforced by
the import-linter contracts in `backend/.importlinter`, not by discipline.
"""
